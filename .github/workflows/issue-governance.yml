# Validates issue structure against the ai-governance work-item templates.
# Not a PR workflow. The implementation lives in vaam-apps/.github.
name: issue governance

on:
  issues:
    types: [opened, edited, reopened]

permissions: {}

jobs:
  structure:
    permissions:
      issues: write
    uses: vaam-apps/.github/.github/workflows/issue-governance.yml@d9c2a1bd5600efab1f0183eeb8e412595a8c5510 # vaam-apps/.github main
    with:
      enforce: true
    secrets:
      app-client-id: ${{ secrets.RELEASE_PLEASE_APP_CLIENT_ID }}
      app-private-key: ${{ secrets.RELEASE_PLEASE_APP_PRIVATE_KEY }}
